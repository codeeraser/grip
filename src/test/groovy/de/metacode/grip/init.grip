import org.hsqldb.Server

init {
    env "hsql", sql(
            url: "jdbc:hsqldb:mem:testdb",
            driver: "org.hsqldb.jdbcDriver",
            user: "sa",
            pwd: "")

    Server.main()
}